
    <%= form_for @player_team, :html => {:class =>"pure-form pure-form-aligned", :multipart => true } do |f| %>
      <% if @player_team.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@player_team.errors.count, "error") %> prohibited this player_team from being saved:</h2>

          <ul>
          <% @player_team.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

    <fieldset>
        <div class="pure-control-group">
            <%= f.label :name %>
            <%= f.text_field :name, :placeholder => "Name" %>
        </div>

        <div class="pure-control-group">
             <%= f.label :position %>
             <%= f.select(:position, PlayerTeam::POSITION) %>
        </div>

        <div class="pure-control-group">
            <%= f.label :desc %>
            <%= f.text_area :desc, :placeholder => "Description", :rows => 10 %>
        </div>

        <div class="pure-control-group">
            <%= f.label :image_upload %>
             <%= f.file_field :avatar %>
        </div>

        <div class="pure-controls">
            <button type="submit" class="pure-button pure-button-primary">Submit</button>
        </div>
    </fieldset>
<% end %>